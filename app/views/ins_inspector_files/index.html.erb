<%= render 'partials/forms/index',
view_name: "Documentos - "+ InsInspector.find(params[:inspector_id]).name,

#field_name, header_name 
table_fields: [['description', 'Arquivo'], ['','','file']] #table used by the index view
%>