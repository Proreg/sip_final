<%= render 'partials/forms/index',
view_name: "Cidades",

#
#field_name, header_name, DB strucutre(If it's a foreign key , regular field, boolean...)
#For Foreign Key, send a symbol instead of the string 
table_fields: [['description', 'Cidade', 'text'],  
 [:sys_state, 'Estado', 'fk']] #table used by the index view
%>